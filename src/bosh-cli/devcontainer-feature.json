{
    "name": "BOSH CLI",
    "id": "bosh-cli",
    "version": "1.0.1",
    "description": "Installs the BOSH CLI, optionally installing additional dependencies.",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "7",
                "none"
            ],
            "default": "latest",
            "description": "Select or enter a BOSH CLI version."
        },
        "install_dependencies": {
            "type": "boolean",
            "default": true,
            "description": "Set to `false` to bypass installing additional dependencies."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
